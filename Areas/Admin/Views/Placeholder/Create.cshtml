@using System.Web.Mvc.Html
@using nl.boxplosive.BackOffice.Mvc.Areas.Admin.Models

@model PlaceholderCreateModel
@{
    ViewBag.Title = @Model.PageTitle;
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <div class="well">
            <div class="form-group">
                @Html.LabelFor(m => m.FieldName, new { @class = "col-sm-3 control-label" })
                <div class="col-sm-5">
                    @Html.TextBoxFor(model => model.FieldName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.FieldName, "", new { @class = "text-danger" })
                </div>
            </div>

            @Html.EditorFor(model => model.Label, new { labelClass = "col-sm-3", textboxClass = "col-sm-5" })

            <div class="form-group">
                @Html.LabelFor(m => m.DataType, new { @class = "col-sm-3 control-label" })
                <div class="col-sm-5">
                    @Html.DropDownListFor(model => model.DataType, true, Model.DataTypes, "Select type", new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.DataType, "", new { @class = "text-danger" })
                </div>
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-12">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </div>
    </div>
}