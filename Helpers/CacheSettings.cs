using System;

namespace nl.boxplosive.BackOffice.Mvc.Helpers
{
    public static class CacheSettings
    {
        public static readonly TimeSpan DefaultRelativeExpiration_Backoffice = TimeSpan.FromHours(1);
    }
}